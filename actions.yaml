add-user:
  description: Add FreshRSS user
  params:
    user:
      type: string
    password:
      type: string
    email:
      type: string
  required: [user]
update-user:
  description: Update existing FreshRSS user
  params:
    user:
      type: string
    password:
      type: string
    email:
      type: string
  required: [user]
